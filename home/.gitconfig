[user]
        email = kaiyadavenport@gmail.com
        name = Kai Davenport
[url "git@github.com:"]
        insteadOf = https://github.com/
[alias]
        c = !"cz commit"
        s = !"git commit -s -S --amend --no-edit"
        cs = !"cz commit; git commit -s -S --amend --no-edit"
[pull]
        rebase = false